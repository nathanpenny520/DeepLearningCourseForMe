# Evaluation & Safety

评估与安全：PPL 实测、能力基准体系、幻觉成因与缓解、越狱 / 提示注入等安全失效模式，收束 LLM 模块。

**运行**：VS Code 打开 `evaluation-safety.ipynb`，内核选 **Python (llm)**。

**关键结论**：PPL ≠ 能力，必须多基准组合评估；幻觉只能压制不能消除；安全是"评估-发现-修补"的持续循环。
