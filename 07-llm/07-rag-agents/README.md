# RAG & Agents

检索增强与智能体：字符袋向量检索演示、RAG 提示组装、Function Calling 与 Agent 循环骨架。

**运行**：VS Code 打开 `rag-agents.ipynb`，内核选 **Python (llm)**。

**关键结论**：RAG 解决知识截止 / 幻觉（先检索再回答）；Function Calling 让模型能行动；Agent = 检索 + 工具 + 记忆 + 规划的循环。
