# VAE

变分自编码器：ELBO 推导、重参数化技巧、2D 潜空间可视化的类别聚类与连续插值生成。

**运行**：VS Code 打开 `vae.ipynb`，内核选 **Python (computer-vision)**。

**关键结论**：ELBO = 重构项 − KL 项；重参数化让采样可导；潜空间连续是"生成"的根基（VQ-VAE 是 Stable Diffusion 的压缩前端）。
