# CV Frontier

前沿回顾：自监督（SimCLR / MAE / CLIP）、生成范式竞争、FID 评估指标直觉实现、多模态与视频展望。

**运行**：VS Code 打开 `cv-frontier.ipynb`，内核选 **Python (computer-vision)**。

**关键结论**：识别走向自监督（CLIP 为底座），生成走向扩散 / 流模型；FID 同时惩罚分布偏移与多样性丢失。
