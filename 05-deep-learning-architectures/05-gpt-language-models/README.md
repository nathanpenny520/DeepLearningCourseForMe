# GPT & Language Models

自回归语言建模、字符级 ToyGPT（Embedding + Block + LM Head）、交叉熵训练、温度与 top-k 生成、玩具到真实 GPT 的对照。

**运行**：VS Code 打开 `gpt-language-models.ipynb`，内核选 **Python (deep-learning-architectures)**。

**前置**：先完成 [04 Transformer Architecture](../04-transformer-architecture)

**关键结论**：亲手训练 + 生成一次，比读十篇架构分析都直观；温度 / top-k 是控制生成质量的两个核心旋钮。
